= form_for committee do |f|
  - if committee.errors.any?
    #error-explanation
      %h2
        = pluralize(committee.errors.count, 'error')
        prohibited this committee from being saved:
      %ul
        - committee.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
